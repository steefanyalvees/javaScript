// array with destructuring 
function rand ([min] = 0, max=100)
{
   // invertendo as variaveis// 
   // if minimo for maior que max, inverta as variaveis 
if (min >max)[min, max] = [max, min] //criando um array

console.log(rand)
}

function age([ageM] = 18, max=50)
  {

if (age < 10) 
  console.log(age)

return Math.floor // aredopndar para baixo. ceil,, para cima 
  }
  